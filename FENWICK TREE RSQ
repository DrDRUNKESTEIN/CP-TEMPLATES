//vector<ll>fenwick(500005, 0);//1 based indexing
//void update(int index, ll value, int n) {
    //while (index <= n) {
        //fenwick[index] = (fenwick[index] + value);
        //index = index + (index & (-index));
    //}
//}
//ll sum(int index) {
	//if(index<1){
		//return 0;
	//}
    //ll ans = 0;
    //while (index > 0)
    //{
		//ans+=fenwick[index];
        //index = index - (index & (-index));
    //}
    //return ans;
//}
